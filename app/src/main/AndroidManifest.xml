<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="haodong.com.views_diy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".prac_animator.Anim1Activity" />
        <activity android:name=".prac_remote_views.DiyPointActivity" />
        <activity android:name=".prac_point.PointActivity"></activity>
        <activity android:name=".paint_prac.PaintActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".myg11n27.G11n27Activity"></activity>
    </application>

</manifest>